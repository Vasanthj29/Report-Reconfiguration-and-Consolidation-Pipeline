# Report-Reconfiguration-and-Consolidation-Pipeline
This script reads raw data from a folder, consolidates into a dataframe, reconfigures the data to match desired standards, and outputs an Excel file.
